[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Frontend service. The backend lives in /server (separate Railway service
# pointed at root directory `server/` with its own Dockerfile/railway.toml).
